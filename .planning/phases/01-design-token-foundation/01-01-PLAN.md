---
phase: 01-design-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "App renders with new design token system without visual errors"
    - "Category colors (blue/orange/green/red) are defined as semantic tokens"
    - "Spacing scale uses 4px base unit with defined touch target sizes"
    - "Typography scale uses 1.333 ratio (perfect fourth) starting at 12px"
    - "Shadows and border radius follow modern SaaS conventions"
    - "Dark mode has separate color definitions with proper contrast"
    - "Inter font is available as CSS variable and mapped to font-sans"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Complete @theme block with design tokens"
      contains: ["--color-category-material", "--spacing-*", "--font-size-*", "--shadow-*", "--radius-*"]
    - path: "src/app/layout.tsx"
      provides: "Inter font with variable CSS setup"
      contains: "variable: '--font-inter'"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable --font-inter mapped to --font-sans"
      pattern: "variable.*font-inter|--font-sans.*var.*font-inter"
    - from: "@theme block"
      to: "Tailwind utility classes"
      via: "Automatic CSS variable generation"
      pattern: "--color-.*--> utilities like bg-.*, text-.*"
---

<objective>
Establish the complete design token foundation in Tailwind CSS v4's @theme block, defining category-preserving colors (자재비/blue, 노무비/orange, 식대/green, 유류비/red), spacing scale, typography scale, shadows, and border radius. Configure Inter variable font with CSS variable mapping for seamless Tailwind integration.

Purpose: Create the visual language that all subsequent phases build upon. This foundation ensures consistency across all screens while preserving business-critical category color recognition.

Output: Complete @theme block in globals.css with all design tokens, Inter font configured as CSS variable.
</objective>

<execution_context>
@/home/ryu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-token-foundation/01-CONTEXT.md
@.planning/phases/01-design-token-foundation/01-RESEARCH.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend @theme block with category color tokens and semantic layer</name>
  <files>src/app/globals.css</files>
  <action>
    Extend the existing @theme block in src/app/globals.css to include:

    **Category Color Tokens (business-critical):**
    ```css
    /* Material (자재비) - Blue */
    --color-category-material-50: oklch(0.97 0.01 264);
    --color-category-material-100: oklch(0.93 0.03 264);
    --color-category-material-500: oklch(0.55 0.22 264);
    --color-category-material-700: oklch(0.45 0.20 264);

    /* Labor (노무비) - Orange */
    --color-category-labor-50: oklch(0.97 0.01 45);
    --color-category-labor-100: oklch(0.93 0.03 45);
    --color-category-labor-500: oklch(0.65 0.20 45);
    --color-category-labor-700: oklch(0.55 0.18 45);

    /* Food (식대) - Green */
    --color-category-food-50: oklch(0.97 0.01 145);
    --color-category-food-100: oklch(0.93 0.03 145);
    --color-category-food-500: oklch(0.65 0.18 145);
    --color-category-food-700: oklch(0.55 0.16 145);

    /* Fuel (유류비) - Red */
    --color-category-fuel-50: oklch(0.97 0.01 25);
    --color-category-fuel-100: oklch(0.93 0.03 25);
    --color-category-fuel-500: oklch(0.55 0.22 25);
    --color-category-fuel-700: oklch(0.45 0.20 25);
    ```

    **Semantic Color Mappings:**
    ```css
    /* Semantic tokens map to category colors */
    --color-primary: var(--color-category-material-500);
    --color-success: var(--color-category-food-500);
    --color-warning: oklch(0.80 0.15 85);
    --color-error: var(--color-category-fuel-500);
    --color-info: var(--color-category-material-500);
    ```

    Preserve existing :root and .dark color definitions. Add dark mode overrides for category colors (lighter variants for contrast).
  </action>
  <verify>npm run build succeeds without CSS parsing errors</verify>
  <done>Category color tokens defined in @theme block, semantic mappings created, dark mode variants added</done>
</task>

<task type="auto">
  <name>Task 2: Add spacing, typography, shadow, and radius scales to @theme block</name>
  <files>src/app/globals.css</files>
  <action>
    Add complete design token scales to the @theme block:

    **Spacing Scale (4px base unit):**
    ```css
    --spacing-0: 0;
    --spacing-px: 1px;
    --spacing-0_5: 0.125rem;   /* 2px */
    --spacing-1: 0.25rem;      /* 4px */
    --spacing-1_5: 0.375rem;   /* 6px */
    --spacing-2: 0.5rem;       /* 8px */
    --spacing-2_5: 0.625rem;   /* 10px */
    --spacing-3: 0.75rem;      /* 12px */
    --spacing-3_5: 0.875rem;   /* 14px - min text size */
    --spacing-4: 1rem;         /* 16px - min touch target */
    --spacing-5: 1.25rem;      /* 20px */
    --spacing-6: 1.5rem;       /* 24px - component padding */
    --spacing-7: 1.75rem;      /* 28px */
    --spacing-8: 2rem;         /* 32px */
    --spacing-9: 2.25rem;      /* 36px */
    --spacing-10: 2.5rem;      /* 40px */
    --spacing-11: 2.75rem;     /* 44px - WCAG touch target */
    --spacing-12: 3rem;        /* 48px - min button height */
    --spacing-14: 3.5rem;      /* 56px - gloves-on button */
    --spacing-16: 4rem;        /* 64px - section spacing */
    --spacing-20: 5rem;        /* 80px */
    --spacing-24: 6rem;        /* 96px */
    ```

    **Typography Scale (1.333 ratio - perfect fourth):**
    ```css
    --font-size-xs: 0.75rem;      /* 12px */
    --font-size-sm: 0.875rem;     /* 14px - gloves-on min */
    --font-size-base: 1rem;       /* 16px - body text */
    --font-size-lg: 1.333rem;     /* 21.33px - subtitle */
    --font-size-xl: 1.777rem;     /* 28.44px - h3 */
    --font-size-2xl: 2.369rem;    /* 37.9px - h2 */
    --font-size-3xl: 3.157rem;    /* 50.5px - h1 */
    --font-size-4xl: 4.209rem;    /* 67.3px - display */

    /* Line heights - generous for readability */
    --leading-xs: 1rem;           /* 16px */
    --leading-sm: 1.25rem;        /* 20px */
    --leading-base: 1.6rem;       /* 25.6px - 1.6 ratio */
    --leading-lg: 1.75rem;        /* 28px */
    --leading-xl: 2rem;           /* 32px */
    --leading-2xl: 2.5rem;        /* 40px */
    --leading-3xl: 3rem;          /* 48px */
    ```

    **Border Radius (modern SaaS convention):**
    ```css
    --radius-sm: 0.25rem;         /* 4px */
    --radius-md: 0.375rem;        /* 6px */
    --radius-lg: 0.5rem;          /* 8px - default */
    --radius-xl: 0.75rem;         /* 12px */
    --radius-2xl: 1rem;           /* 16px */
    ```

    **Shadows (subtle, professional):**
    ```css
    --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
    --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
    --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
    --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
    ```

    Add to existing @theme block, preserving all existing color definitions.
  </action>
  <verify>npm run build succeeds, Tailwind utilities like p-4, text-lg, rounded-lg, shadow-md work correctly</verify>
  <done>Spacing, typography, radius, and shadow scales defined in @theme block, Tailwind utilities auto-generated</done>
</task>

<task type="auto">
  <name>Task 3: Update Inter font to use CSS variable pattern</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <action>
    **In src/app/layout.tsx:**
    Add `variable` option to Inter font configuration:
    ```tsx
    const inter = Inter({
      subsets: ["latin"],
      variable: '--font-inter',  // Add this line
      display: 'swap',
    });
    ```

    Apply the variable to the html element:
    ```tsx
    <html lang="ko" className={inter.variable}>
    ```

    **In src/app/globals.css @theme block:**
    Add font mapping to connect Tailwind's font-sans to the Inter variable:
    ```css
    @theme {
      /* Map Tailwind's font-sans utility to Inter variable */
      --font-sans: var(--font-inter);
    }
    ```

    This creates the connection: Inter variable → CSS variable → Tailwind utility class.
  </action>
  <verify>npm run dev starts without errors, browser DevTools shows --font-inter loaded and applied</verify>
  <done>Inter font configured with CSS variable, mapped to Tailwind font-sans utility</done>
</task>

</tasks>

<verification>
Overall phase verification:
- App builds and runs without errors
- Design tokens generate valid Tailwind utilities
- Category colors are defined and accessible
- Inter font variable is properly mapped
- Dark mode colors are defined separately
</verification>

<success_criteria>
1. App builds successfully (npm run build completes without errors)
2. All design tokens are defined in @theme block (colors, spacing, typography, shadows, radius)
3. Category color tokens preserve business-critical distinction (blue/orange/green/red)
4. Inter font is available as CSS variable and mapped to font-sans
5. Spacing scale follows 4px base unit with touch target sizes defined
6. Typography scale uses 1.333 ratio starting at 12px
7. Dark mode has separate color definitions in .dark selector
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-token-foundation/01-01-SUMMARY.md` with:
- What was implemented (category colors, spacing, typography, shadows, Inter variable)
- Key decisions made (OKLCH values, type scale, base unit)
- Files modified
- Next steps (install skeleton library, create skeleton components, validate contrast)
</output>
