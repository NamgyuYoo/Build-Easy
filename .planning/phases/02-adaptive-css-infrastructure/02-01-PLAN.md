---
phase: 02-adaptive-css-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Touch devices can use larger buttons via pointer-coarse variant"
    - "Mouse devices can use normal-sized buttons via pointer-fine variant"
    - "Dev server starts without CSS compilation errors"
    - "Build completes successfully (no CSS errors)"
  artifacts:
    - path: "package.json"
      provides: "Tailwind CSS v4.1.18 dependency declaration"
      contains: '"tailwindcss": "^4.1.18"'
    - path: "package-lock.json"
      provides: "Locked Tailwind CSS v4.1.18 dependency"
      contains: '"tailwindcss": "4.1.18"'
    - path: "node_modules/tailwindcss/package.json"
      provides: "Installed Tailwind CSS v4.1.18 binary"
      min_lines: 10
  key_links:
    - from: "package.json"
      to: "npm install"
      via: "dependency installation"
      pattern: "npm install"
    - from: "globals.css"
      to: "Tailwind CLI"
      via: "CSS compilation"
      pattern: "@import.*tailwindcss"
---

<objective>
Upgrade Tailwind CSS from v4.0.0 to v4.1.18 to enable native `pointer-coarse` and `pointer-fine` variants for adaptive touch/mouse detection.

**Purpose:** Tailwind v4.1+ (released April 2025) includes built-in support for pointer media queries without needing custom `@custom-variant` implementations. This upgrade is required before implementing adaptive CSS infrastructure in plans 02 and 03.

**Output:** Upgraded Tailwind dependency with native pointer variants available for use in subsequent plans.
</objective>

<execution_context>
@/home/ryu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-adaptive-css-infrastructure/02-RESEARCH.md

# Prior phase: Design Token Foundation
Phase 1 established the @theme block with color tokens, spacing, typography, and shadows. All existing styles remain backward compatible.

# Research finding: Tailwind v4.1 critical feature
From 02-RESEARCH.md:
> **Critical discovery:** Tailwind CSS v4.1 (April 2025) added **native** `pointer-coarse` and `pointer-fine` variants. This eliminates the need for custom `@custom-variant` implementations for pointer detection.

# Current version check
package.json currently shows: `"tailwindcss": "^4.0.0"`
Required: `"tailwindcss": "^4.1.18"`
</context>

<tasks>

<task type="auto">
  <name>Upgrade Tailwind CSS to v4.1.18</name>
  <files>package.json</files>
  <action>
    1. Run: `npm install tailwindcss@^4.1.18 @tailwindcss/postcss@^4.1.18`
    2. Verify both packages upgrade to v4.1.18 (PostCSS plugin must match Tailwind version)
    3. Do NOT modify any other dependencies
    4. Do NOT use --legacy-peer-deps (React 19 compatibility resolved in Phase 1)

    Expected output:
    - tailwindcss@4.1.18 installed
    - @tailwindcss/postcss@4.1.18 installed
    - No audit vulnerabilities related to Tailwind
  </action>
  <verify>
    Run: `cat package.json | grep -A1 -B1 '"tailwindcss"'`
    Expected: `"tailwindcss": "^4.1.18"`

    Run: `npm run build`
    Expected: Build completes with exit code 0
  </verify>
  <done>
    Tailwind CSS upgraded to v4.1.18, build succeeds, no CSS compilation errors
  </done>
</task>

<task type="auto">
  <name>Verify native pointer variants are available</name>
  <files>src/app/globals.css</files>
  <action>
    1. Create a test file with pointer variants to verify Tailwind recognizes them:
       ```bash
       echo '<button class="pointer-coarse:h-14 pointer-fine:h-10">Test</button>' > /tmp/pointer-test.html
       ```
    2. Run dev server: `npm run dev` (allow 5 seconds for startup)
    3. Check for CSS compilation errors in output
    4. If Tailwind v4.1.18 is correctly installed, pointer variants will compile without errors
    5. Clean up test file: `rm /tmp/pointer-test.html`

    Do NOT modify globals.css in this task (verification only)
  </action>
  <verify>
    Run: `npm run dev` (check first 10 lines of output for errors)
    Expected: No "Unknown utility" or "Cannot resolve" errors related to pointer variants

    Alternative check: `grep -r "pointer-coarse" node_modules/tailwindcss/package.json`
    Expected: File contains "pointer" variant references (v4.1.18 feature)
  </verify>
  <done>
    Native pointer variants are available for use in subsequent plans
  </done>
</task>

</tasks>

<verification>
Run verification checklist after all tasks complete:

1. **Version check:**
   ```bash
   npm list tailwindcss @tailwindcss/postcss
   ```
   Expected: Both show v4.1.18

2. **Build check:**
   ```bash
   npm run build
   ```
   Expected: Exit code 0, no CSS errors

3. **Dev server check:**
   ```bash
   npm run dev
   ```
   Expected: Server starts on port 3000, no CSS compilation errors

4. **CSS import check:**
   ```bash
   head -5 src/app/globals.css
   ```
   Expected: `@import "tailwindcss";` present (no changes needed)
</verification>

<success_criteria>
- [ ] Tailwind CSS upgraded to v4.1.18 (package.json confirms)
- [ ] PostCSS plugin upgraded to matching v4.1.18
- [ ] Build completes successfully with no CSS errors
- [ ] Dev server starts without deprecation warnings
- [ ] Native pointer variants are available (verified via test)
- [ ] No breaking changes to existing styles (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/02-adaptive-css-infrastructure/02-01-SUMMARY.md` with:
- Actual versions installed
- Any build warnings encountered
- Confirmation of pointer variant availability
- Breaking changes assessment (expected: none)
</output>
